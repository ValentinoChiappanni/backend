# Ejemplo de variables de entorno para el backend
# Copiar este archivo a .env y ajustar los valores según sea necesario

# Puerto del servidor backend
PORT=3000

# URL de conexión a la base de datos
DATABASE_URL="postgresql://user:password@localhost:5432/administration_db?schema=public"

# CORS - Orígenes permitidos (separados por coma si son múltiples)
CORS_ORIGIN=http://localhost:5173
